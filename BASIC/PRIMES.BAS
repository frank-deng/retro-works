10 DEFINT A-Z:DIM P(2049):C=0:P(0)=3
20 INPUT "Max: ",M
22 IF M>=2 THEN PRINT USING "########";2;
23 IF M>=3 THEN PRINT USING "########";3;
30 FOR N=3 TO M STEP 2
40 FOR I=0 TO C
50 IF (N MOD P(I))=0 THEN GOTO 100
60 IF N<(P(I)*P(I)) THEN GOTO 80
70 NEXT I
80 PRINT USING "########";N;
90 IF C<2048 THEN C=C+1:P(C)=N
100 NEXT N
110 END

